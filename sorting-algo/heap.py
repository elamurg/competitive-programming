"""
Heap Sort is a comparison-based sorting algorithm that uses a binary heap data 
structure. It works by first building a max heap from the input array, then 
repeatedly extracting the maximum element from the heap and placing it at the 
end of the sorted array. The time complexity of Heap Sort is O(n log n) in all cases 
(best, average, and worst), making it more efficient than Selection Sort for large lists. 
However, it is not stable, meaning that equal elements may not maintain their relative 
order after sorting.
"""