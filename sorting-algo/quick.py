"""
Quick Sort is a divide-and-conquer sorting algorithm that works by selecting a 'pivot' element 
from the array and partitioning the other elements into two sub-arrays according to whether 
they are less than or greater than the pivot. The sub-arrays are then sorted recursively. 
The time complexity of Quick Sort is O(n log n) in the average case and O(n^2) in the worst 
case, making it less predictable than Merge Sort. However, it is often faster in practice due 
to its in-place sorting nature and good cache locality.
"""